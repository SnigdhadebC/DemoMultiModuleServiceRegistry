server:
  port: 8081

spring:
  application:
    name: product-service

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka

feign:
  hystrix:
    enabled: true


management:
  endpoints:
    web:
      exposure:
        include: '*'


